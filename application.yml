spring:
  application:
    name: config-default
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch_registry: true
    register-whth-eureka: true
    service-url:
      defaultZone: http://localhost:8080/eureka
